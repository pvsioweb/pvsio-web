init?(st: State): bool = (st = init(0))
trans(pre, post: State): bool{{#if transitions}} =
   {{#each transitions}}(per_{{{this}}}(pre) IMPLIES post = {{{this}}}(pre)){{#unless @last}} AND{{/unless}}
   {{/each}}
{{/if}}
