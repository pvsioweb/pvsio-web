{{#if template_description}}
%
%   Description: This template generates C enumerated types. They are created using a typedef:
%                - typedef struct { c1, c2, ... } enum_type_name;
%
{{/if}}{{#if comment}}/*
 * {{comment}}
 */{{/if}}{{#each enumtype}}
typedef enum {{#if constructors}}{ {{#each constructors}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} } {{name}};{{/if}}{{/each}}
