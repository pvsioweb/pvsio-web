REVERSIBILITY: THEOREM
  FORALL (pre, post: State):{{#each actions}}
    (per_{{{this}}}(pre) AND post = {{{this}}}(pre))
      IMPLIES (per_{{{../reversingAction}}}(post) AND {{{../reversingAction}}}(post)`{{{../s}}} = pre`{{{../s}}}){{#unless @last}} AND{{/unless}}{{/each}}
