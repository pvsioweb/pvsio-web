<div class="form-center">
    <div class="panel panel-info shadow center">
        <div class="panel-heading">{{header}}</div>
        <form class="center form-horizontal" onsubmit="return false">
            <div class="panel-body" style="overflow-y: scroll; height: 180px;">
                <div class="form-group">
                    <label class="control-label col-sm-4" for="currentLabel">{{textLabel}}</label>
                    <div class="col-sm-8">
                        <select id="currentLabel"
                                class="form-control"
                                type="text"
                                value=""
                                name="transition"
                                required="true">
                            {{#each currentLabels}}
                            <option value="{{this}}">{{this}}</option>
                            {{/each}}
                        </select>
                    </div>
                </div>
                <div class="form-group">
                    <label class="control-label col-sm-4" for="newLabel">New label</label>
                    <div class="col-sm-8">
                        <input id="newLabel"
                               class="form-control"
                               type="text"
                               value=""
                               placeholder="Please enter new label..."
                               name="newLabel"
                               required="true"
                               style="padding: 6px 12px;"/>
                        <script>document.getElementById("newLabel").select();</script>  
                    </div>
                </div>
            </div>
            <div class="panel-footer">
                <button type="button" class="btn btn-danger left" id="btnLeft">{{buttons.[0]}}</button>
                <button type="submit" class="btn btn-success right" id="btnRight">{{buttons.[1]}}</button>
            </div>
        </form>
    </div>
</div>